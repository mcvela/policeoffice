server:
    port: 8080

spring:
    profiles:
        active: dev
    datasource:
        dataSourceClassName: org.postgresql.Driver
        url: jdbc:postgresql://bandit-db:5432/bandit
        username: postgres
        password: postgres

naming_strategy: org.springframework.boot.orm.jpa.hibernate.SpringNamingStrategy
